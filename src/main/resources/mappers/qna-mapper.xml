<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="qnaMapper">
	
	<!-- 목록 조회 -->
	<select id="qnaList" resultType="qnaDTO">
		select  seq_qna, nickname, qna_title, qna_written_date
			from tbl_qna
			order by seq_qna
	</select>
	
	<insert id="insertQuestion" parameterType="qnaDTO">
		insert into tbl_qna 
			seq_qna, id, nickname, qna_title, qna_content, qna_written_date, answer
		values
			(#{seq_qna}, #{id}, #{nickname}, #{qna_title}, #{qna_content}, sysdate, null)
	</insert>
	
	<update id="insertAnswer" parameterType="String">
		update tbl_qna set 
		answer = #{answer}
		where seq_qna = #{seq_qna}
	</update>
	
	<select id="selectOne" resultType="qnaDTO">
		select * from tbl_qna
		where seq_qna = #{seq_qna}
	</select>
	
	<select id="selectById" resultType="qnaDTO">
		select #{seq_qna}, #{id}, #{nickname}, #{qna_title}, #{qna_written_date}
		from tbl_faq
		where id = #{id}
	</select>
	
	<delete id="delete">
		delete from tbl_qna
			where seq_qna = #{seq_qna}
	</delete>
</mapper>